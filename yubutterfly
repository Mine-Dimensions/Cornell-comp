butterfly = ""
inp = input("Does the butterfly have a stripe? Type \"Has a stripe\" or \"Does not have a stripe\"\n")
if inp == "Has a stripe":
    butterfly = "Viceroy"
elif inp == "Does not have a stripe":
    butterfly = "Monarch"
else:
    butterfly = "Invalid input. Type exactly what was asked."

print(butterfly)
